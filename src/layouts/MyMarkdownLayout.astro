---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title} <h1>{frontmatter.title}</BaseLayout>
<p>Written by: {frontmatter.author} on {frontmatter.pubDate}</p>
<slot />
<BaseLayout />
